


export const customMuiSelect={
    styleOverrides: {
        root: (({theme}: any) => ({
            padding: `7px`,
            minHeight:31,
            color: theme.palette.neutral.contrastText,
            fontWeight: 600,
            fontSize: 12,
            lineHeight: 15 + 'px',
        }))
    },
}as object